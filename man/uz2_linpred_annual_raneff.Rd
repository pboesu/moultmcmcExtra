% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uz2_annual_raneff.R
\name{uz2_linpred_annual_raneff}
\alias{uz2_linpred_annual_raneff}
\title{Bayesian inference for the Type 2 moult model with annual random intercepts (no individual recaptures)}
\usage{
uz2_linpred_annual_raneff(
  moult_index_column,
  date_column,
  start_formula = ~1,
  duration_formula = ~1,
  sigma_formula = ~1,
  year_factor_column,
  raneff_components = c("start", "duration"),
  flat_prior = FALSE,
  lumped = FALSE,
  beta_sd = 0,
  data,
  init = "auto",
  log_lik = TRUE,
  standata_only = FALSE,
  ...
)
}
\arguments{
\item{moult_index_column}{the name of the column in \code{data} containing moult indices,
i.e. a numeric vector of (linearised) moult scores: 0 = old plumage, 1 = new plumage}

\item{date_column}{the name of the column in \code{data} containing sampling dates, encoded
as days since an arbitrary reference date (numeric vector)}

\item{start_formula}{model formula for start date}

\item{duration_formula}{model formula for duration}

\item{sigma_formula}{model formula for start date sigma}

\item{year_factor_column}{the name of a factor column in \code{data} containing year identifiers}

\item{raneff_components}{character vector specifying which linear predictors receive
annual random intercepts. Any combination of \code{"start"} (start date) and \code{"duration"}.
Defaults to \code{c("start", "duration")} (both).}

\item{flat_prior}{logical; use flat (uniform) priors on start date and duration
intercepts? Defaults to FALSE (weakly informative normal priors)}

\item{lumped}{logical; use the lumped (Type 2L) likelihood? Defaults to FALSE}

\item{beta_sd}{use zero-centred normal priors for regression coefficients other than
intercepts? If <= 0 Stan's default improper flat priors are used}

\item{data}{input data frame}

\item{init}{specification of initial values. Can be \code{"auto"} or any permitted
\code{rstan::sampling} option}

\item{log_lik}{logical; retain pointwise log-likelihood in output? Defaults to TRUE}

\item{standata_only}{logical; if TRUE, return the Stan data list without fitting}

\item{...}{arguments passed to \code{rstan::sampling} (e.g. iter, chains)}
}
\value{
When \code{standata_only = FALSE}, an object of class \code{moultmcmc}; otherwise the
Stan data list
}
\description{
Fits a Type 2 (Underhill-Zucchini) moult model with annual random intercepts on start
date and duration. No individual-level random effects; use when within-season recaptures
are absent. Type 2 data include birds in old plumage, active moult, and new plumage.
}
